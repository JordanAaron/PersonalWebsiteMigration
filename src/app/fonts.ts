import { Kumbh_Sans, Quicksand, Urbanist } from 'next/font/google'

export const body = Urbanist({ subsets: ['latin'] })
export const heading = Quicksand({ subsets: ['latin'] })
export const logo = Kumbh_Sans({ subsets: ['latin'] })
